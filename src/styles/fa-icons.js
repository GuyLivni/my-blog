import fontawesome from "@fortawesome/fontawesome";
import brands from "@fortawesome/fontawesome-free-brands";

const faIconLibrary = () => fontawesome.library.add(brands);

export default faIconLibrary;
